------

# 1 安装（30 秒）

把整个 `flow-wtg/` 文件夹复制到你的新项目根目录：

```
your-project/
├── flow-wtg/          ← 这里
│   ├── METHODOLOGY.md
│   ├── RULES.md
│   ├── prompts/
│   └── templates/
├── src/
└── ...
```

## 1.1 Claude Code 配置

在 Claude Code 中，直接在对话框说

### 1.1.1 包装 skills

把每个 prompt 包成 skill。`@flow-wtg/prompts\`。示例：`flow-wtg/prompts/0-change.md` → `.claude/skills/flow-change/SKILL.md`

### 1.1.2 包装斜杠命令

将项目根目录下的 `flow-wtg\prompts\*.md` 每个 md 文件，作为 Claude Code 的命令 `@.claude/commands\`

### 1.1.3 注入全局规则

把 `flow-wtg/SYSTEM.md` 的内容**复制一次**到 IDE 全局规则文件，永久注入。

------

# 2 从零做一个新项目

假设你周末想做一个工具小站。打开 Cursor / Claude Code / 通义灵码 / 随便哪个 IDE，对话里打：

```
@flow-wtg/GO.md 我想做一个可以把 markdown 转成海报的小工具
```

**它会带你走 8 步：**

| 步骤 | 阶段                         | 说明                                                         |
| ---- | ---------------------------- | ------------------------------------------------------------ |
| 1    | **0-change**                 | 自动生成一个 change-id（比如 `md-to-poster`），给你这次需求建个档案目录 `.specs/md-to-poster/` |
| 2    | **1-requirement**            | 反问你几个关键问题（谁用 / 核心场景 / 验收标准 / 非功能要求），产出一份 `REQUIREMENT.md`，里面是可打勾的验收标准 |
| 3    | **2-design**                 | 先给你 5 张技术栈卡片（Next.js 全栈 / Vite + Express / Astro / ...）让你选一个，选完再出 `DESIGN.md` + 必要的 ADR（架构决策记录） |
| 4    | **2a-ui-design**             | 如果是前端项目，加这一步。先出视觉设计（调性 / 主色 / 字体 / 圆角规范），再写代码 |
| 5    | **3-task**                   | 把设计拆成 5-15 分钟能搞完的原子任务，标好哪些能并行，生成 `TASK.md` |
| 6    | **4-dev**                    | **每个任务一个 fresh context 去跑**，TDD 开头，verify 结尾，跑完写 `SUMMARY.md` |
| 7    | **5-test**                   | 多轮测试（单元 / 集成 / 边界 / 迁移）                        |
| 8    | **6-review → 7-integration** | 三轮 code review + 归档 + 把教训沉淀到 `LESSONS.md`          |
|      |                              |                                                              |

用户介入点

听起来很长。实际上你作为用户只需要在 **3 个地方** 介入：

- **第 1 步** — 回答几个业务问题
- **第 3 步** — 选技术栈（也可以直接让 AI 推荐，说"你看着办"）
- **第 6 步** — review 的时候看一下

其余时间它自己跑，你该干嘛干嘛。

------

# 3 给老项目加个功能

这个场景才是我自己每天用的。分两种情况。

## 3.1 第一次在这个老项目用 flow-wtg

```
@flow-wtg/GO.md 帮我加一个通知中心
```

它会先停下来：

```
🔍 检测到本项目已有以下 AI 上下文文档：
  - AGENTS.md（仓库根，4.2K）
  - .cursor/rules/architecture.md（2.8K）

请选择：
  1. 综合现有文档 + 代码扫描，生成 CONTEXT.md（推荐）
  2. 以现有文档为准，跳过扫描
  3. 跳过 + 不读现有文档（不推荐）
```

选 1 会额外花 15-50k tokens 做一次**入场扫描**，把你项目的技术栈、命名风格、既有抽象（HTTP 客户端 / 状态管理 / utils / hooks）、禁动清单全扒一遍，写进 `CONTEXT.md`。

之后所有 change 都读它。这一步是**一次性投资**。

扫完继续正常的 8 步流程。但此时有 6 条专门给老项目的护栏会激活：

| 护栏   | 名称           | 说明                                                         |
| ------ | -------------- | ------------------------------------------------------------ |
| **B1** | 入场扫描       | 就是刚才说的这一步                                           |
| **B2** | 架构对齐       | 设计阶段强制列清"本次会碰哪些既有模块 / 沿用哪些既有抽象 / 引入哪些新模式" |
| **B3** | 边界约束       | 每个任务声明 `read_files`（允许看）+ `write_files`（允许改）。提交前自动 `git diff` 对比，越界强制回滚 |
| **B4** | 破坏性变更门槛 | 删代码 ≥ 5 行 / 改公共接口，必须先 grep 整库引用图，贴给我看，等我确认才动 |
| **B5** | 沿用抽象 grep  | 写新代码前，每个能力都要先 grep 一遍项目里有没有，找到就用，找不到才另起 |
| **B6** | 视觉语汇对齐   | （前端 change 才触发）— 加新页面 / 新组件前先 grep 既有的 token / hover 样式 / 缓动曲线 / elevation / 图标库 / 文案调性，写观察报告给我校准。让新加的 UI 和原有 UI 视觉上无法区分。详见后面 UI 那一节 |

## 3.2 之后再加功能

```
@flow-wtg/GO.md 再加一个用户档案页
```

直接读 `CONTEXT.md` 进 0-change。不再打扰你。

# 4 什么时候用？

| 场景                               | 推荐                                      |
| ---------------------------------- | ----------------------------------------- |
| 一次性脚本 / hackathon / 50 行以内 | 不用 flow-wtg，裸跑                       |
| 新项目 MVP（周末做完那种）         | 极简模式（删 review 和 test）             |
| 生产代码 / 老项目加功能            | 完整 flow-wtg                             |
| 团队协作 / 交接频繁                | 完整 flow-wtg                             |
| 金融 / 医疗 / 安全关键             | 完整 flow-wtg + 所有护栏 + 外部 lint 工具 |

------

# 作者

- **名称**：吴特刚
- **公司**：秦云公司
